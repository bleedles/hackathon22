;(function() {
	'use strict';
	
    var app = angular.module("contractFactory", []);
    app.factory('contracts', contracts);

    /* @ngInject */
    function accountInfo($http) {
    	
    }
})();